import React from 'react';

const AdminDashboard: React.FC = () => {
    return (
        <div>
            <h1 className="mb-4">Admin Dashboard</h1>
            <p>Đây là trang tổng quan dành cho Admin.</p>
            {/* Thêm các biểu đồ và thống kê ở đây */}
        </div>
    );
};

export default AdminDashboard;